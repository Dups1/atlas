/// Configuracion unica del backend (Render). Todas las clases deben usar [urlBase].
class ConfigBackend {
  ConfigBackend._();

  static const String urlBase = 'https://backend-atlas-gwxq.onrender.com';
}
